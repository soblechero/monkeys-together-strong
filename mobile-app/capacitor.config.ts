import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.soblechero.mts',
  appName: 'Monkeys Together Strong',
  webDir: 'dist'
};

export default config;
